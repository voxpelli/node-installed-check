name: "CodeQL"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '44 1 * * 3'

jobs:
  analyze:
    uses: voxpelli/ghatemplates/.github/workflows/codeql-analysis.yml@main
