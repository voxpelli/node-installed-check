# Basic Example

This is a simple example showing how `installed-check` works with a basic Node.js project.

## Usage

```bash
# From the repository root
npm install
node cli-wrapper.cjs examples/basic
```

This will check that all dependencies in this example's `package.json` comply with the specified requirements.
